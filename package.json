{
  "name": "trader-intelligence",
  "version": "0.1.0",
  "description": "AI-powered trade journaling + LLM pattern analysis. Classify trades with Claude (setup, time-of-day, quality, mistakes) and deliver daily summaries.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "classify": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@supabase/supabase-js": "^2.45.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
